// =============================================================
// Step 01 — 가장 단순한 람다 (캡처 없음)
// =============================================================
// 목표:
//   1) 인자를 받지 않고 "Hello, Lambda!" 를 출력하는 람다 `hello` 를 만든다.
//   2) 두 개의 int 를 받아 합을 반환하는 람다 `add` 를 만든다.
//   3) 한 개의 int 를 받아 제곱을 반환하는 람다 `square` 를 만든다.
//      (반환 타입을 명시적으로 적어볼 것: ` -> int`)
//
// 제약:
//   - 캡처 절은 비워둔다 ([])
//   - 전역/멤버 함수는 만들지 말고 main 안에서만 정의/사용
//
// Expected Output (정확히 이 5줄):
// Hello, Lambda!
// add(3, 4) = 7
// square(5) = 25
// add(10, 20) = 30
// square(-6) = 36
// =============================================================

#include <iostream>

int main() {
    // TODO: hello, add, square 람다를 정의하세요.

    // 아래 호출부는 그대로 두세요.
    hello();
    std::cout << "add(3, 4) = "    << add(3, 4)    << '\n';
    std::cout << "square(5) = "    << square(5)    << '\n';
    std::cout << "add(10, 20) = "  << add(10, 20)  << '\n';
    std::cout << "square(-6) = "   << square(-6)   << '\n';
    return 0;
}
